# 🚀 Инструкция: Загрузка в Git и запуск на другом компьютере

## ✅ Что уже готово

- ✅ `.gitignore` - настроен для исключения больших файлов
- ✅ `README.md` - описание проекта
- ✅ `SETUP.md` - подробная инструкция по установке
- ✅ `QUICK_START.md` - быстрая инструкция
- ✅ Все скрипты и конфигурации готовы

## 📤 ШАГ 1: Загрузка в Git (на этом компьютере)

### Вариант A: Если репозиторий уже существует

```bash
cd /Users/nurlykhan/fight_detect/third_try/other_version

# Добавить все новые файлы
git add .

# Коммит
git commit -m "Add YOLO training scripts and dataset preparation"

# Отправить в репозиторий
git push origin main
```

### Вариант B: Создание нового репозитория

```bash
cd /Users/nurlykhan/fight_detect/third_try/other_version

# 1. Добавить все файлы
git add .

# 2. Коммит
git commit -m "Initial commit: Fight detection YOLO training project"

# 3. Создать репозиторий на GitHub:
#    - Зайдите на https://github.com
#    - Нажмите "New repository"
#    - Название: fight-detection-yolo (или любое другое)
#    - НЕ добавляйте README, .gitignore, лицензию

# 4. Подключить remote
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_REPO.git

# 5. Отправить
git branch -M main
git push -u origin main
```

### ⚠️ Важно о размере

- Датасет: ~173MB
- Это нормально для Git, но загрузка может занять 5-10 минут
- Если нужно быстрее, используйте Git LFS (см. ниже)

### Git LFS для ускорения (опционально)

```bash
# Установить Git LFS
git lfs install

# Отслеживать изображения
git lfs track "dataset/**/*.jpg"
git lfs track "yolo_dataset/**/*.jpg"

# Добавить изменения
git add .gitattributes
git commit -m "Add Git LFS for images"
git push
```

---

## 📥 ШАГ 2: Установка на другом компьютере

### 1. Клонирование

```bash
git clone https://github.com/ВАШ_USERNAME/ВАШ_REPO.git
cd ВАШ_REPO
```

### 2. Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать
# Linux/Mac:
source venv/bin/activate
# Windows:
# venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Если есть GPU с CUDA:
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

### 3. Проверка датасета

```bash
# Проверить что датасет на месте
ls -la dataset/
# Должны быть папки: bent_over, covering_face, face_slap, и т.д.
```

### 4. Подготовка датасета для YOLO

```bash
python prepare_yolo_dataset.py
```

Это создаст:
- `yolo_dataset/images/train/` и `yolo_dataset/images/val/`
- `yolo_dataset/labels/train/` и `yolo_dataset/labels/val/`
- `yolo_dataset/data.yaml`

### 5. Настройка обучения

Откройте `train_yolo.py` и настройте:

```python
EPOCHS = 100              # Количество эпох
BATCH_SIZE = 16          # Уменьшите до 8 если не хватает памяти
device = 0               # 0 для GPU, 'cpu' для CPU
MAX_WORKERS = 8          # Количество ядер CPU
```

**Для GPU:**
```python
device = 0  # или 'cuda:0'
```

**Для CPU:**
```python
device = 'cpu'
MAX_WORKERS = os.cpu_count()  # Использовать все ядра
```

### 6. Запуск обучения

```bash
python train_yolo.py
```

### 7. Мониторинг

```bash
# В другом терминале - смотреть прогресс
tail -f fight_detection/fight_detection_yolo11n/results.csv

# Проверить использование GPU (если есть)
watch -n 1 nvidia-smi
```

---

## 📊 Что будет загружено в Git

✅ **Будет загружено:**
- Весь код проекта (Python скрипты)
- Датасет с изображениями и метками (~173MB)
- Конфигурационные файлы
- Requirements.txt
- Документация

❌ **НЕ будет загружено** (благодаря .gitignore):
- Результаты обучения (`fight_detection/`)
- Кэши (`*.cache`)
- Модели (`*.pt`)
- Виртуальные окружения (`venv/`)
- Логи

---

## 🔧 Решение проблем

### Проблема: "CUDA out of memory"

**Решение:**
```python
# В train_yolo.py уменьшите:
BATCH_SIZE = 8  # или даже 4
IMG_SIZE = 416  # вместо 640
```

### Проблема: Медленное обучение на CPU

**Решение:**
- Используйте GPU если доступен
- Уменьшите количество эпох для тестирования
- Увеличьте `BATCH_SIZE` если есть память

### Проблема: Ошибка импорта модулей

**Решение:**
```bash
pip install --upgrade -r requirements.txt
pip install --upgrade numpy pandas
```

### Проблема: Датасет не найден

**Решение:**
```bash
# Проверить структуру
ls -la dataset/
ls -la yolo_dataset/

# Пересоздать yolo_dataset
rm -rf yolo_dataset
python prepare_yolo_dataset.py
```

---

## ✅ Чеклист перед отправкой

- [ ] Все файлы добавлены: `git status`
- [ ] Датасет на месте: `ls dataset/`
- [ ] `.gitignore` настроен правильно
- [ ] `requirements.txt` актуален
- [ ] README.md обновлен

---

## 📝 После обучения

Модель будет сохранена в:
```
fight_detection/fight_detection_yolo11n/weights/best.pt
```

Использование:
```python
from ultralytics import YOLO

model = YOLO('fight_detection/fight_detection_yolo11n/weights/best.pt')
results = model('image.jpg')
results[0].show()
```

---

## 💡 Советы

1. **Для GPU:** Используйте `device = 0` для максимальной скорости
2. **Для CPU:** Установите `MAX_WORKERS = os.cpu_count()` для использования всех ядер
3. **Мониторинг:** Следите за метриками в `results.csv`
4. **Early stopping:** Модель остановится автоматически если нет улучшения 20 эпох

---

Готово! 🎉

