# 🚀 Простая инструкция: Публикация в новом репозитории

## Быстрый способ (3 команды)

### 1. Создайте новый репозиторий на GitHub
- Зайдите на https://github.com
- Нажмите "New repository"
- Название: например `fight-detection-yolo-training`
- **НЕ добавляйте** README, .gitignore, license
- Нажмите "Create repository"

### 2. Выполните команды:

```bash
cd /Users/nurlykhan/fight_detect/third_try/other_version

# Удалить старый remote
git remote remove origin

# Добавить новый (ЗАМЕНИТЕ URL на ваш!)
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_REPO.git

# Добавить все файлы и отправить
git add .
git commit -m "Add YOLO training project with dataset"
git branch -M main
git push -u origin main
```

**Или используйте готовый скрипт:**

```bash
cd /Users/nurlykhan/fight_detect/third_try/other_version
./PUBLISH_TO_NEW_REPO.sh
```

---

## 📋 Что будет отправлено

✅ **Будет отправлено:**
- Весь код проекта
- Датасет (~173MB)
- Все скрипты и конфигурации
- Документация

❌ **НЕ будет отправлено** (благодаря .gitignore):
- Результаты обучения (`fight_detection/`)
- Модели (`*.pt`)
- Кэши
- Виртуальные окружения

---

## ⚠️ Важно

1. **Размер:** Датасет ~173MB - загрузка займет 5-10 минут
2. **Git LFS:** Для ускорения можно использовать Git LFS (см. PUBLISH_NEW_REPO.md)
3. **Проверка:** После отправки проверьте репозиторий на GitHub

---

## 🔍 Проверка

После отправки проверьте:
```bash
# Проверить remote
git remote -v

# Должен показать ваш новый репозиторий
```

---

Готово! 🎉

